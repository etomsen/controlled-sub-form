'use strict';
var expect = require('chai').expect;
var index = require('../dist/index.js');

describe('@daubihe/controlled-sub-form tests', () => {
    it('should return true', () => {
        expect(true).to.equal(true);
    });
});